name: test
on: push
  jobs:
    test:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - name: Build
          run: docker-compose up -d --build
        - name: Test
          run: docker exec -it app make test-unit
